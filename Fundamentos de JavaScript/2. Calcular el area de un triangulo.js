//console.log(`El area de un triangulo de base 5 y altura 7 es: ${5*7/2}`);
let base = 5
let altura = 7
//console.log(`El area de un triangulo de base ${base} y ${altura} es: ${base*altura/2}`);

      // Crear funciones en javaScript
// function triangleArea(base,altura) {
//   return base*altura/2
// }
// console.log(`El area de un triangulo de base
// ${base} y ${altura} es: ${triangleArea(base,altura)}`);

    // Asignar una funcion fecha a una variable

const triangleArea= (base,altura) => base*altura/2
console.log(`El area de un triangulo de base
${base} y ${altura} es: ${triangleArea(base,altura)}`);


// si definimos una variable con la palabra const el valor no puede ser alterado en la ejecucion
// ya que esta va a ser una funcion
